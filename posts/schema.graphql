extend type Subscription {
"""
- chat app live time data
"""
chat: Message
}

extend type Mutation {
    post(title: String!, content: String, owner_id:Int!): String!
}
type Message {
    sender: String
    message: String
  }
